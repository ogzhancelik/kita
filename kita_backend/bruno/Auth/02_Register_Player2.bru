meta {
  name: 02 Register Player 2
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "oyuncu2",
    "email": "oyuncu2@kita.com",
    "password": "password123"
  }
}

script:post-response {
  const body = res.getBody();
  if (body && body.token) {
    bru.setVar("token2", body.token);
  }
  if (body && body.user && body.user.id) {
    bru.setVar("userId2", body.user.id);
  }
}


