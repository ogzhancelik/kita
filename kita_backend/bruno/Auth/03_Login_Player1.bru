meta {
  name: 03 Login Player 1
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username_or_email": "oyuncu1",
    "password": "password123"
  }
}

script:post-response {
  const body = res.getBody();
  if (body && body.token) {
    bru.setVar("token1", body.token);
  }
  if (body && body.user && body.user.id) {
    bru.setVar("userId1", body.user.id);
  }
}


